---
name: Add trustedProxies and MEMORY.md bootstrap to gateway startup
status: open
created: 2026-03-22T02:31:39Z
updated: 2026-03-22T02:31:39Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Add trustedProxies and MEMORY.md bootstrap to gateway startup

## Description

Modify the gateway startup script in `06-openclaw-core.yaml` to:
1. Set `gateway.trustedProxies` in openclaw.json to reduce WebSocket disconnects and event gap warnings
2. Create a default MEMORY.md in the agent workspace if it doesn't exist

## Acceptance Criteria
- [ ] `gateway.trustedProxies: ["10.42.0.0/16"]` added to openclaw.json initial config
- [ ] MEMORY.md created at `/home/node/.openclaw/workspace/MEMORY.md` on startup
- [ ] Existing MEMORY.md not overwritten if already present
- [ ] Event gap warnings reduced or eliminated after trustedProxies set

## Technical Details

File: `openclaw-k3s-paas/k8s-manifests/06-openclaw-core.yaml`

1. Add `"trustedProxies":["10.42.0.0/16"]` to the gateway section of the initial JSON config
2. Add after LINE token injection block:
```bash
# Bootstrap MEMORY.md for agent workspace
mkdir -p /home/node/.openclaw/workspace
[ -f /home/node/.openclaw/workspace/MEMORY.md ] || cat > /home/node/.openclaw/workspace/MEMORY.md << 'MEMEOF'
# Agent Memory

This file stores persistent knowledge across conversations.

## System Info
- Platform: OpenClaw K3s PaaS
- Domain: cindytech1-openclaw.woowtech.io
MEMEOF
echo '[init] MEMORY.md bootstrapped'
```

## Dependencies

None — can run in parallel with tasks 001 and 003.

## Effort Estimate
- Size: S
- Hours: 0.5
